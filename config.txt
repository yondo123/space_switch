[변수]
1. 인턴 멤버 배열 
    - memberList = [{name,divId,seatId}]
    - 1,3 = 1분단 3번째
    - 자리배치 할 배열

2. 임시 멤버 배열
    - 자리배치를 후 다음 순서에 중복 안되게 기존 배열과 비교할 배열
    - 임시 멤버 배열이기 때문에 추후 원소 삭제
    - tempList = [{name,preDivId,preSeatId}]
    - 이미 앉았던 분단은 제외한다 (우선 제외)
        - 1분단-> 2,3 -> 
        - 2분단-> 1,3..
    - 이미 지정된 좌석은 배치할 수 없다.

[로직]
1. 자리배치 버튼
    1.1 validDiviston()
        - posDiv에 이동가능한 분단 값을 삽입
        - 랜덤함수를 통해 갈 수 있는 좌표 생성
        - 해당 좌표와 curseat[]배열과 비교후 다르면 최종저장
    1.2 randomSet() 함수
        - posDiv에 이동가능한 분단 값을 삽입
        - 랜덤함수를 통해 갈 수 있는 좌표 생성
        - 해당 좌표와 curseat[]배열과 비교후 다르면 최종저장
        - 이후 li에 값 뿌림(좌표대로)

2. 자리섞기 버튼
    2.1 validDiviston()
        - posDiv에 이동가능한 분단 값을 삽입
        - 랜덤함수를 통해 갈 수 있는 좌표 생성
        - 해당 좌표와 curseat[]배열과 비교후 다르면 최종저장
    2.2 randomSet() 함수
        - posDiv에 이동가능한 분단 값을 삽입
        - 랜덤함수를 통해 갈 수 있는 좌표 생성
        - 해당 좌표와 curseat[]배열과 비교후 다르면 최종저장
        - 이후 li에 값 뿌림(좌표대로)
        
3. 테스트
    3.1 최소 10번까지 테스트해보고 오류를 잡아라!

4. 작업 상황
    - temp 배열 순서 바꾸어주기!!!!!